---
name: github
description: Use to interact with branches, make pull requests and push code
---

Both `/back` and `/front` have separate repositories that you can interact with.

- Use `git` commands to checkout, commit and track files
- Use `gh` to create pull requests
- NEVER merge PRs without permission
- Leave descriptive commit messages